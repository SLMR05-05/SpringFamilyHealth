server:
    port: 8080
    servlet:
        context-path: /FamilyHealth

spring:
    datasource:
        url: "jdbc:mysql://localhost:3306/FamilyHealth"
        username: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
